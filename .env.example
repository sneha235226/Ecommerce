PORT=
MONGO_URI=
JWT_SECRET=
JWT_EXPIRES_IN=

ADMINS_JSON=

# Nodemailer / SMTP settings
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Frontend URL — used to build the verification link in emails
CLIENT_URL=
